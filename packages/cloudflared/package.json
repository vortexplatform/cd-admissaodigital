{
  "name": "@repo/cloudflared",
  "private": true,
  "scripts": {
    "dev": "TOKEN=$(grep '^CLOUDFLARE_TUNNEL_TOKEN=' ../../.env.development | cut -d= -f2-) && cloudflared tunnel --no-autoupdate run --token \"$TOKEN\""
  }
}
